# MCA 26.1.2 Parity Regression Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the nine confirmed 26.1 port regressions and adapt authority commit `4493a7af1` so `dev/26.1.2` matches current `dev/1.21.1` behavior on Minecraft 26.1.2 APIs.

**Architecture:** Keep each repair in its existing owner and use the smallest 26.1-specific adaptation needed to preserve authority behavior. Use common unit tests for pure policy/clock/ticket contracts and NeoForge GameTests for live entity, brain, equipment, player-dimension, persistence, and archer movement behavior.

**Tech Stack:** Java 21, Gradle, Minecraft 26.1.2 patched sources, Fabric/NeoForge multi-loader project, JUnit 5, MCA NeoForge GameTest compatibility layer.

**Spec:** `docs/superpowers/specs/2026-09-14-26.1.2-parity-regression-repair-design.md`

## Global Constraints

- `dev/1.21.1` is the behavioral authority.
- `C:\Users\Mik\Downloads\MCA\local-source\local-sources\minecraft-patched-26.1.2.76-sources` is the Minecraft 26.1 API authority.
- Preserve intentional 26.1-specific pathfinding, movement, fishing, mixin, and data-driven rendering fixes.
- Do not reintroduce JourneyMap-specific mixins or bridge code.
- Do not push.
- Keep live transient brain memories runtime-only; do not make them serializable to solve refresh loss.
- Keep current `family_tree` as the 26.1 SavedData ID while accepting legacy `MCA-FamilyTree.dat`.
- Destiny teleport loading must expire after five ticks and must not use `PLAYER_LOADING`.

---

### Task 1: FamilyTree migration and Destiny ticket lifetime

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/util/WorldUtils.java`
- Modify: `common/src/main/java/net/conczin/mca/server/world/data/FamilyTree.java`
- Modify: `common/src/main/java/net/conczin/mca/network/c2s/DestinyMessage.java`
- Test: `common/src/test/java/net/conczin/mca/util/WorldUtilsLegacyDataTest.java`
- Test: `common/src/test/java/net/conczin/mca/network/c2s/DestinyTicketTest.java`

**Interfaces:**
- `WorldUtils.loadData(...)` gains an overload accepting explicit legacy IDs while retaining existing callers.
- `FamilyTree.get(ServerLevel)` requests current ID `family_tree` with legacy alias `MCA-FamilyTree`.
- `DestinyMessage` owns a runtime-only `new TicketType(5L, TicketType.FLAG_LOADING)` constant; it is non-persistent because it does not carry `FLAG_PERSIST`.

- [ ] **Step 1: Add failing legacy-ID tests**

```java
@Test
void familyTreeLegacyCandidateIncludesOriginalId() {
    assertEquals(List.of("family_tree", "MCA-FamilyTree"),
            WorldUtils.savedDataLookupIds("family_tree", "MCA-FamilyTree"));
}

@Test
void duplicateLegacyIdsAreIgnored() {
    assertEquals(List.of("family_tree"),
            WorldUtils.savedDataLookupIds("family_tree", "family_tree"));
}
```

- [ ] **Step 2: Run the focused common test and confirm RED**

Run: `.\gradlew :common:test --tests net.conczin.mca.util.WorldUtilsLegacyDataTest --console=plain`

Expected: compile/test failure because explicit legacy candidate support does not exist.

- [ ] **Step 3: Implement alias-aware SavedData fallback**

Add a deterministic candidate helper and overload `loadData` so current data is always checked first, then legacy aliases in order. Cache migrated legacy data under the current `SavedDataType`.

```java
static List<String> savedDataLookupIds(String currentId, String... legacyIds) {
    return Stream.concat(Stream.of(currentId), Arrays.stream(legacyIds))
            .distinct()
            .toList();
}
```

Change `FamilyTree.get(...)` to pass `"MCA-FamilyTree"` as its legacy alias.

- [ ] **Step 4: Run the legacy-ID test and confirm GREEN**

Run: `.\gradlew :common:test --tests net.conczin.mca.util.WorldUtilsLegacyDataTest --console=plain`

- [ ] **Step 5: Add a failing Destiny ticket contract test**

```java
@Test
void destinyTicketIsExpiringLoadingTicket() {
    TicketType ticket = DestinyMessage.destinyTeleportTicket();
    assertEquals(5L, ticket.timeout());
    assertTrue(ticket.doesLoad());
    assertFalse(ticket.doesSimulate());
    assertNotSame(TicketType.PLAYER_LOADING, ticket);
}
```

- [ ] **Step 6: Run the Destiny ticket test and confirm RED**

Run: `.\gradlew :common:test --tests net.conczin.mca.network.c2s.DestinyTicketTest --console=plain`

- [ ] **Step 7: Use an MCA five-tick runtime loading ticket**

Define package-visible `DESTINY_TELEPORT_TICKET = new TicketType(5L, TicketType.FLAG_LOADING)` in `DestinyMessage` and replace `PLAYER_LOADING` in `handleBlockPos` while retaining radius `1`.

- [ ] **Step 8: Run both focused tests and compile common**

Run: `.\gradlew :common:test --tests net.conczin.mca.util.WorldUtilsLegacyDataTest --tests net.conczin.mca.network.c2s.DestinyTicketTest :common:compileJava --console=plain`

- [ ] **Step 9: Commit**

```text
fix: restore saved-data and teleport ticket parity
```

---

### Task 2: Greeting clock and registry-based tombstones

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/brain/VillagerBrain.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/relationship/Relationship.java`
- Test: `common/src/test/java/net/conczin/mca/entity/ai/MemoriesTest.java` or nearest existing memory test
- Test: `common/src/test/java/net/conczin/mca/entity/relationship/RelationshipTombstoneTest.java`

**Interfaces:**
- New memories use `player.level().getOverworldClockTime()`.
- Tombstone resolution accepts short MCA IDs and namespaced registered `TombstoneBlock` IDs.

- [ ] **Step 1: Add a failing day-basis test**

```java
@Test
void memoryDayUsesWorldClockTicks() {
    Memories memories = new Memories(null, 24000L * 7L, UUID.randomUUID());
    assertEquals(7L, memories.getLastSeen());
}
```

Add package-visible `VillagerBrain.memoryClockTime(Player)` returning `player.level().getOverworldClockTime()` and make `getMemoriesForPlayer` use it. Exercise the actual call site in the live brain GameTest from Task 4.

- [ ] **Step 2: Confirm RED for the call-site contract, then change the call site**

Run the focused test, then replace `player.level().getGameTime()` with `player.level().getOverworldClockTime()` only in new `Memories` creation.

- [ ] **Step 3: Add failing tombstone resolution tests**

Cover `cross_headstone`, `mca:cross_headstone`, malformed/unknown IDs, a non-tombstone block, and a registry-resolved tombstone.

- [ ] **Step 4: Confirm RED and implement registry lookup**

Port authority `getConfiguredTombstoneState()` semantics to 26.1 `Identifier`/`BuiltInRegistries.BLOCK` APIs and retain cross-headstone fallback.

- [ ] **Step 5: Run focused tests and commit**

```text
fix: restore calendar and tombstone config parity
```

---

### Task 3: Inworld relationship safeguards and infertility command guard

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/chatAI/inworldAIModules/RelationshipModule.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/chatAI/modules/RelationModule.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/interaction/VillagerCommandHandler.java`
- Test: `common/src/test/java/net/conczin/mca/entity/ai/chatAI/inworldAIModules/RelationshipModuleTest.java`
- Test: focused interaction test or NeoForge GameTest for procreation command gating

**Interfaces:**
- Romantic Inworld fields are zeroed for child-age villagers and relatives while trust/respect/familiar continue to apply.
- `procreate` rejects `Traits.INFERTILE` before cooldown/start.

- [ ] **Step 1: Add a failing pure relationship-delta test**

Extract/test a package-visible calculation helper so the test can exercise real weighting without server mocks.

```java
assertEquals(5, RelationshipModule.calculateHeartDelta(update(10, 10, 10, 10, 10), false));
assertEquals(3, RelationshipModule.calculateHeartDelta(update(10, 10, 10, 10, 10), true));
```

Here the boolean means romantic fields are suppressed; the exact expected division follows production weighting.

- [ ] **Step 2: Confirm RED, then port authority suppression and relative prompt**

Use `AgeState` and `Relationship.IS_RELATIVE` exactly as authority does. Add `$villager is related to $player.` in `RelationModule`.

- [ ] **Step 3: Add failing infertility gating coverage**

Create the smallest test seam around the command eligibility decision or a live GameTest that gives a married villager `Traits.INFERTILE`, invokes the command path, and asserts no procreation/cooldown start.

- [ ] **Step 4: Confirm RED, then restore the authority guard**

Place the infertility branch between the hearts check and `mayProcreateAgain/startProcreating`, retaining the authority translation key.

- [ ] **Step 5: Run focused tests and commit**

```text
fix: restore relationship and fertility safeguards
```

---

### Task 4: Preserve live brain state and FOLLOW combat ownership

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/entity/VillagerEntityMCA.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/brain/VillagerBrain.java`
- Test: `neoforge/src/main/java/net/conczin/mca/entity/ai/brain/tasks/ArcherCombatMovementGameTests.java` or new focused brain GameTest class

**Interfaces:**
- `refreshBrain(ServerLevel)` preserves live memory values without routing the live copy through codecs/NBT.
- `setMoveState(FOLLOW, leader)` avoids destructive refresh and preserves active guard combat targeting.

- [ ] **Step 1: Add failing GameTests**

Add one test that seeds codec-less MCA memories plus vanilla `ATTACK_TARGET`, `LOOK_TARGET`, and `WALK_TARGET`, calls `refreshBrain`, and verifies values remain. Add one test that switches an attacking guard to FOLLOW and verifies the live attack/look/walk target survives.

- [ ] **Step 2: Run the focused GameTest selection/server and confirm RED**

Use the existing NeoForge GameTest runner with the new batches; confirm failure is the dropped memories/FOLLOW refresh.

- [ ] **Step 3: Implement target-compatible live-memory preservation**

Prefer a 26.1 direct brain-copy API if patched sources expose one. Otherwise snapshot registered live memory optionals from the old brain and reapply them to the rebuilt profile without serializing through `brain.pack()`.

- [ ] **Step 4: Match authority FOLLOW behavior**

Port the authority `setMoveState` structure: FOLLOW sets `PLAYER_FOLLOWING`, clears STAY, abandons job, skips brain reset, and restores combat look/walk target for a live guard target.

- [ ] **Step 5: Run focused GameTests plus common compile and commit**

```text
fix: preserve live villager brain state
```

---

### Task 5: Restore recovery-food item-use lifecycle

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/entity/VillagerEntityMCA.java`
- Test: new NeoForge `VillagerRecoveryFoodGameTests.java`

**Interfaces:**
- Recovery use owns a small state machine: active flag, use ticks/hand, inventory-source flag, previous dominant-hand stack, completion guard.
- Completion/cancellation restores previous equipment exactly once.

- [ ] **Step 1: Add failing live GameTests**

Cover: sword + inventory food -> food consumed -> same sword restored; danger cancellation -> sword restored/no double consume; active danger -> recovery never starts.

- [ ] **Step 2: Confirm RED**

Run the new GameTest batch and verify current immediate-eat path fails equipment preservation/timing.

- [ ] **Step 3: Port the authority state machine to 26.1 item-use APIs**

Use current 26.1 `DataComponents.FOOD`/consumable APIs and preserve target dangerous-food filtering. Keep `clearChoreItem` unchanged.

- [ ] **Step 4: Run recovery GameTests and compile both loaders**

Run: `.\gradlew :common:compileJava :fabric:compileJava :neoforge:compileJava --console=plain`

- [ ] **Step 5: Commit**

```text
fix: restore villager recovery food lifecycle
```

---

### Task 6: Restore player join dimension refresh/sync

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/server/ServerInteractionManager.java`
- Test: `common/src/test/java/net/conczin/mca/server/ServerInteractionManagerTest.java`

**Interfaces:**
- Enabled `scalePlayerHitboxWithSizeAndWidth` refreshes `ServerPlayer` dimensions after MCA player data is available and rebroadcasts set entity data.
- Disabled config keeps current no-extra-refresh behavior.

- [ ] **Step 1: Add a failing focused test around the join decision**

Add package-visible `ServerInteractionManager.shouldRefreshPlayerDimensions()` returning `Config.getInstance().scalePlayerHitboxWithSizeAndWidth`; the test toggles the config field around this helper and restores its previous value in `finally`. Keep actual `refreshDimensions()` and sync in `onPlayerJoin`.

- [ ] **Step 2: Confirm RED, then port authority join block to current network APIs**

Use current `PlayerSaveData` and `Network.sendToPlayer(new PlayerDataMessage(...))` APIs already present in 26.1.

- [ ] **Step 3: Run focused test, common compile, and commit**

```text
fix: refresh scaled player dimensions on join
```

---

### Task 7: Port archer checkpoint `4493a7af1`

**Files:**
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/MCAMoveControl.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/brain/tasks/ArcherMovementTask.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/brain/tasks/BowTask.java`
- Modify: `common/src/main/java/net/conczin/mca/entity/ai/brain/tasks/RangedCombatPositioning.java`
- Modify: `common/src/test/java/net/conczin/mca/entity/ai/brain/tasks/ArcherMovementStateTest.java`
- Modify: `neoforge/src/main/java/net/conczin/mca/entity/ai/brain/tasks/ArcherCombatMovementGameTests.java`

**Interfaces:**
- `MCAMoveControl.strafe(float forwards, float right, double speedModifier)` supplies explicit direct-strafe speed.
- KITE uses group-aware escape search and normal `0.5` path speed.
- Direct strafe claims movement by clearing walk target/stopping navigation.
- `BowTask` does not force locomotion yaw during draw.

- [ ] **Step 1: Port/adapt only the checkpoint tests first**

Bring over authority tests for pre-combat walk-target replacement, group kite safety, group fallback rejection, one-block-raised escape, bow facing ownership, navigation/path speed, direct strafe speed, hold/navigation cleanup, and updated cadence. Adapt only API/package differences needed for 26.1 GameTest infrastructure.

- [ ] **Step 2: Run archer unit/GameTests and confirm RED**

Run the common archer unit tests first, then the NeoForge GameTest runner. Record the failing checkpoint contracts before touching production movement code.

- [ ] **Step 3: Port `MCAMoveControl` and timing/ownership changes**

Adapt the authority hunk semantically to 26.1. Preserve target `ON_TOP_OF_TRAPDOOR` and climb/pathfinding behavior.

- [ ] **Step 4: Port group escape and raised candidate search**

Adapt `RangedCombatPositioning` using current 26.1 navigation/path APIs and `LandRandomPos.movePosUpOutOfSolid` equivalent from patched sources.

- [ ] **Step 5: Port facing ownership**

Remove direct `lookAt` ownership from `BowTask` while retaining `LOOK_TARGET` tracking in archer movement.

- [ ] **Step 6: Run archer tests to GREEN and commit**

```text
feat: port latest archer combat movement
```

---

### Task 8: Documentation cleanup and full parity verification

**Files:**
- Modify: `docs/superpowers/specs/2026-09-14-26.1.2-parity-regression-repair-design.md` to keep approved status and clean Markdown whitespace
- Modify: this plan to mark executed tasks after verification

- [ ] **Step 1: Recheck authority head**

Run: `git log -1 --oneline dev/1.21.1` and `git log --oneline 4493a7af1..dev/1.21.1`.

If production commits appeared, audit them before declaring parity.

- [ ] **Step 2: Run required common tests and compilation**

```text
.\gradlew :common:test --console=plain
.\gradlew compileJava processResources --console=plain
```

- [ ] **Step 3: Run complete NeoForge GameTests**

Run: `.\gradlew :neoforge:runGameTestServer --console=plain`

- [ ] **Step 4: Run full build**

Run: `.\gradlew build --console=plain`

- [ ] **Step 5: Verify packaging and repository hygiene**

```text
git diff --check
git diff --name-only --diff-filter=U
rg -n "^(<<<<<<<|=======|>>>>>>>)" -g '!build/**' -g '!local-sources/**'
jar tf neoforge/build/libs/mca-neoforge-26.1.2-SNAPSHOT.jar
```

Confirm the NeoForge jar contains no `net/conczin/mca/neoforge/gametest/**` or `*GameTests*.class` entries and target-specific `LookAtTargetSink`/pathfinding/class-tweaker fixes remain present.

- [ ] **Step 6: Final implementation commit**

Commit any verification-driven/doc cleanup with a narrow message. Do not push.
