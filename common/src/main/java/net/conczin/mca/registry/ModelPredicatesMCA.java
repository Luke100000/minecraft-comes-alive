package net.conczin.mca.registry;

public interface ModelPredicatesMCA {
    static void setup() {
        // Item model predicates were replaced by item model property definitions in 26.1.
        // Keep this as a no-op until MCA custom item models are migrated.
    }
}
